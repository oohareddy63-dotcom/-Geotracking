{
  "name": "geotracking",
  "version": "1.0.0",
  "description": "Employee Work Tracking System with Geo-Fencing",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client1 && npm install && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client1 && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client1 && npm start\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.6",
    "multer": "^2.0.2"
  },
  "author": "",
  "license": "ISC"
}
